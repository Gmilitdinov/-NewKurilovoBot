{
  "name": "checkers-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "deploy": "git add . && git commit -m \"Auto-deploy: Update from local changes\" && git push origin main",
    "watch": "nodemon --watch . --ext js,css,html --exec \"npm run deploy\""
  },
  "dependencies": {
    "express": "^4.17.1",
    "ws": "^8.2.3",
    "mongodb": "^4.1.3",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "16.x"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
} 